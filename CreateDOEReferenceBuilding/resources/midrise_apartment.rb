
def add_geometry(model)
  
  
  #load geometry from the saved .osm
  self = safe_load_model("#{Dir.pwd}/midrise_apartment_geometry_loads_and_schedules.osm")

end
 
def add_loads(model)

end
 
def add_hvac(model)
 
end
